- name: Install nginx
  ansible.builtin.apt:
    name: nginx # for web traffic and certbot
    state: present
    update_cache: true

- name: Removing the default nginx site config exists
  ansible.builtin.file:
    path: /etc/nginx/sites-enabled/default
    state: absent
